# Image generation provider: "openai" (default) or "demo" (no API calls, local mockup only)
IMAGE_PROVIDER=openai

# OpenAI API key (used when IMAGE_PROVIDER=openai)
OPENAI_API_KEY=sk-...

# Optional overrides
OPENAI_IMAGE_MODEL=gpt-image-1
OPENAI_IMAGE_SIZE=1024x1024
OPENAI_IMAGE_QUALITY=medium
# How many reference coin photos from ./references to send with each render (0 to disable)
REFERENCE_COUNT=4

# Server
PORT=3000
# Max generations per IP per hour
RATE_LIMIT_PER_HOUR=20

# ---- Ordering ----
# Optional price list as JSON: coin size -> [[minQty, unitPriceUSD], ...]
# Without it, no prices are shown and orders are saved as quote requests.
# PRICE_TABLE={"1.5":[[1,9.5],[50,5.95],[100,4.95],[250,3.95],[500,3.45]],"1.75":[[1,10.5],[50,6.45],[100,5.45],[250,4.45],[500,3.95]],"2":[[1,11.5],[50,6.95],[100,5.95],[250,4.95],[500,4.45]],"2.5":[[1,13.5],[50,8.45],[100,7.45],[250,6.45],[500,5.95]],"3":[[1,15.5],[50,9.95],[100,8.95],[250,7.95],[500,7.45]]}
# Stripe secret key enables "Pay Now" via Stripe Checkout (needs PRICE_TABLE too)
# STRIPE_SECRET_KEY=sk_live_...
# Public URL of the deployed app, used for Stripe return links
# PUBLIC_URL=https://coins.example.com
# Optional: every order is POSTed as JSON to this URL (Zapier, Make, Zoho Flow, etc.)
# ORDER_WEBHOOK_URL=https://hooks.zapier.com/...
